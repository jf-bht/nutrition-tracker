# Optional: eigener kostenloser USDA FoodData Central Key
# (ohne diese Variable wird automatisch der oeffentliche DEMO_KEY genutzt -
# funktioniert sofort, aber mit niedrigerem, geteiltem Rate-Limit)
# Kostenlos in ~1 Minute erhalten: https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=
